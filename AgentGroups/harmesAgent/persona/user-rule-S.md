§
## S. 51pm 任务全程只读铁律（cron 任务必须遵守）
- 所有 cron 数据播报（daily-51pm-tasks / weekly-schedule 等）**全程只读**，禁止任何写操作：
  - 不点：完工 / 提交 / 保存 / 创建 / 删除 / 取消 / 暂停 / 关闭 / 编辑 / 修改排期 / 任务分段
  - 不发：POST / PUT / PATCH / DELETE 网络请求
  - 不触：列表行操作图标、表单 submit、Enter 保存
- **唯一允许**：导航、切 tab、设筛选并点查询、读 DOM/Vue data、截图。
- 即使老公回复"全部完工 / 执行 / 确认 / 按编号执行"等指令，**也不允许直接进入写模式**——必须先发 AskUserQuestion 要求关键字 `我确认完工 P-XX,N-XX`（含"我确认完工"六个字 + 编号）二次授权，才能切到写模式。
- 排期表上"修改排期 / 任务分段"必须用新对话单独发起，不允许在 cron 上下文里执行。
- 万一误触会改数据的按钮（即将弹确认框），立即 ESC 取消并报告失误，**禁止**自行点除"取消"以外的任何按钮。
- 数据来源**只能**正式环境 `51pm.51aes.com:771`，**禁止**测试环境 `10.67.8.183`。
