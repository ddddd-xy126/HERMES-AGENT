# 51PM — 版本验收（依开发内容走流程验收，产出验收报告 + 截图，供发版技能使用）

> **半自动验收 skill**。输入是开发者的本周开发内容（可能很糙），agent 扮演真实用户在 51PM 里把每个功能流程走一遍，检测 BUG 与内容不合理，边走边截图，最终产出结构化验收报告。报告 + 截图交给发版技能（`51pm-release-notes`，位于 `D:\project\51PM发版\51PM-Version\.github\skills\`）生成发版内容初稿，最后由用户人工定稿。

## 适用场景

- "验收本周开发内容：……"
- "帮我验收 V2.2.9，开发完成了这些：……"
- "测试环境走一遍这几个新功能，看有没有问题"

## ⚠️ 与其他 51pm skill 的关键差异：必须走真实 UI 交互

README 里"首选 Vue data 直写"的规则**在本 skill 中反转**：

| 用途 | 允许的手段 |
|---|---|
| 操作功能（点击、输入、切换、拖拽） | **只能走真实 DOM 交互**（`click` / `type` / 键盘事件）——验收的目的就是验证 UI 交互本身，Vue 直写会把待验证的交互层整个绕过去，等于没验 |
| 核对数据（列表条数、字段值、状态） | 可以用 `js()` 读 Vue data 作为辅助断言，但不能代替 UI 操作 |

如果一个按钮用真实点击点不动——**这本身就是一个 🐛BUG 发现**，记录下来，不要换 Vue 直写绕过去。

## 参数

| 参数 | 示例 | 说明 |
|---|---|---|
| `env` | `test` / `prod` | **验收默认测试环境** `10.67.8.183:7777`（有"当前为开发环境"水印）。用户明确说了才用正式环境；正式环境下所有写操作逐项先问 |
| `version` | `V2.2.9` | 本次验收的目标版本号，用于截图目录命名 |
| `dev_notes` | （文本） | 开发者的本周开发内容，允许口语化、不完整 |
| `output_dir` | `agent-workspace/acceptance/V2.2.9/` | 截图与报告落盘目录，默认按版本号建 |

## 前置（强制）

1. **Tab 复用模板**：执行 [README.md → 操作前置规则](README.md#操作前置规则强制)，禁止盲目 `new_tab`。
2. **登录**：走企微 OAuth。遇到登出状态**暂停问用户**，不代输凭据。
3. **环境确认**：用户没写明环境时，`AskUserQuestion` 问清楚（验收一般在测试环境，但要确认这批功能是否已部署到测试环境）。

## 流程

### 第 1 步：把开发内容拆成验收清单（先确认再动手）

对 `dev_notes` 逐条解析，产出验收清单，每项包含：

```
- 功能名：（从开发内容里提炼）
- 推测入口：「一级菜单-二级页面」（参考 README 主模块表；拿不准就标"待现场找"）
- 计划流程：作为真实用户会怎么走（3~8 步）
- 观察点：这条功能"对/不对"分别长什么样
```

**把清单发给用户确认一次**（飞书场景直接回消息），确认后再开始操作。这一步防止对开发内容理解跑偏，白跑一轮。

### 第 2 步：逐项走流程 + 截图

每个验收项按"计划流程"用真实 UI 操作走一遍，检查维度：

| 维度 | 看什么 |
|---|---|
| 功能正确性 | 按钮/入口存在、点击有响应、流程能走通、无报错弹窗、无控制台红错 |
| 数据正确性 | 数字对得上（可交叉核对：看板汇总 vs 明细列表条数）、状态流转正确 |
| 交互体验 | 加载有反馈、操作路径顺不顺、有没有点了没反应/重复提交 |
| 内容合理性 | 文案有无歧义/错别字、字段口径怪不怪、与相邻功能是否矛盾 |
| 边界情况 | 空数据展示、超长文本溢出、翻页、筛选组合 |

**截图规范**（用 harness 的 `screenshot()`，落到 `output_dir`）：

```
{序号:02d}-{功能名}-{步骤描述}.png     # 过程图：每个关键步骤（进入前/操作中/结果页）
{序号:02d}-{功能名}-bug-{简述}.png    # 异常图：发现问题必截，截到问题现场
final-{功能名}.png                    # 定妆图：功能最完整、最干净的一屏，给发版文档用
```

### 第 3 步：结论分级

每个验收项给一个结论：

- ✅ **通过** — 流程走通、各维度无异常
- 🐛 **BUG** — 必须给出：复现步骤（从哪个页面开始逐步列）、预期 vs 实际、截图、严重度（阻断 / 严重 / 一般 / 轻微）
- ⚠️ **需人工复核** — agent 验不了或拿不准的，写明原因：
  - 权限不够（当前账号看不到该页面/数据）
  - 时间/后端触发逻辑（如"超两周自动转暂停中"只能验展示层）
  - 视觉细节拿不准（截图给用户判断）
  - 不确定是 BUG 还是设计如此

### 第 4 步：产出验收报告

在 `output_dir` 写 `acceptance-report.md`，固定模板：

```markdown
# 51PM {version} 验收报告
- 验收时间：YYYY-MM-DD HH:mm
- 环境：测试 10.67.8.183:7777
- 验收账号角色：XXX
- 总览：N 项验收，✅x / 🐛y / ⚠️z

## 1. {功能名} — ✅通过
- 入口：「统计-项目人员看板」
- 实走流程：1)… 2)… 3)…
- 截图：01-xxx-进入.png、01-xxx-下钻.png、final-xxx.png
- 推断需关注人员：PM、组长
- 【发版素材】支持XXX，支持XXX，方便XXX；   ← 按发版技能的句式预写一版

## 2. {功能名} — 🐛BUG
- 复现步骤：…
- 预期：… / 实际：…
- 截图：02-xxx-bug-数据不一致.png
- 严重度：严重

## 3. {功能名} — ⚠️需人工复核
- 原因：当前账号无 DTA 管理员权限，看不到模型数据看板明细
- 已验部分：入口存在、页面能打开（截图 03-xxx-入口.png）

## 验收产生的测试数据（待清理）
- 在测试环境创建了任务「xxx」（用于验证 xx 流程），如需清理请告知

## 交接给发版技能
定妆图清单 + 各功能【发版素材】段，已可直接作为 51pm-release-notes 的输入。
```

### 第 5 步：交接发版技能（可选，问用户）

- 报告与截图目录路径发回用户（飞书场景发文件/路径）。
- 用户确认后，把 `final-*.png` 定妆图拷贝到发版仓库：`/mnt/d/project/51PM发版/51PM-Version/发版最新资源截图/`（WSL 路径），供发版技能引用。**拷贝前先问，不要自动写入发版仓库。**

## 安全红线

1. 验收会产生写操作（建任务、改状态等）——**测试环境**允许直接做，但产生的测试数据必须记录在报告"待清理"一节；**正式环境**每个写操作前单独问用户。
2. 不删除任何已有数据，包括测试环境。
3. 验收中遇到登出、企微扫码、二次验证：暂停，问用户。
4. 两套环境 host 不外发（不写进给外部看的文档/截图标注）。

## 已知待迭代项（第一轮实测后回填）

- [ ] 各统计看板的确切入口路径（首轮由 agent 现场找，找到后写死到本节）
- [ ] 验收账号的角色与可见范围清单
- [ ] 常见页面的 `wait_for_selector` 锚点（Element UI 表格/图表加载慢时的等待策略）
